# Copy to `.env` and fill values locally.
#
# Optional Weights & Biases
WANDB_API_KEY=
WANDB_PROJECT=
WANDB_RUN_NAME=

# Optional Hugging Face (if you hit gated models / rate limits)
HF_TOKEN=

# Logging / misc
TRANSFORMERS_VERBOSITY=info
TOKENIZERS_PARALLELISM=false
